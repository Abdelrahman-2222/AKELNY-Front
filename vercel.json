{
  "buildCommand": "npm install --include=dev && npm run build",
  "outputDirectory": "dist/AKELNY-Front",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
